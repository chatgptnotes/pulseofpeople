[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Skipping build commands'"]

[start]
# Start gunicorn without migrations - database connection not required at startup
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --timeout 120"
