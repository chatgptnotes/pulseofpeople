[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Skipping build commands'"]

[start]
cmd = "python manage.py migrate --noinput && python manage.py create_default_user && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
